fastapi>=0.109.0
uvicorn[standard]>=0.27.0
netmiko>=4.4.0
pydantic>=2.5.0
pyyaml>=6.0.1
aiofiles>=23.2.1
cryptography>=41.0.7
pydantic-settings>=2.1.0
requests>=2.31.0

# 规则文件的往返编辑（保留注释）—— 规则库写回用
ruamel.yaml>=0.19

# 测试：TestClient 走真实路由（型号含 "/" 的回归测试）需要 httpx
pytest>=8.0
httpx>=0.27
